server {
    listen 80;

    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
        # This line is the fix:
        try_files $uri $uri/ /index.html;
    }

    # Optional: Handle errors with index.html as well
    error_page   500 502 503 504  /50x.html;
    location = /50x.html {
        root   /usr/share/nginx/html;
    }
}